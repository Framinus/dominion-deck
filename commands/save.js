// this is where the save function will be kept.

const save = function() {
  // read from savedGames.json
  // let idCount = the length of the savedGames array + 1
  // push an object:
  // {id: idCount, set: array}
  // write to the savedGames.json file. 
};

module.exports = save;
